# OpenRA-RL-Util 功能报告

## 1. 目录整体架构

本仓库是一个名为 `openra-rl-util` 的 Python 工具库（版本 0.1.0），为 **OpenRA-RL 强化学习生态**提供共享基础组件。它本身不包含游戏引擎或 RL 训练代码，而是提供连接"游戏环境"与"RL 算法"的中间层能力。

整个目录围绕三种核心能力组织：

| 能力 | 对应模块 | 用途 |
|------|----------|------|
| 游戏静态数据 | `damage_matrix.py` | 单位/建筑的护甲、造价、武器克制关系（红警数据） |
| 训练信号 | `reward_vector.py` | 逐 tick 的 7+1 维多维度奖励向量 |
| 评估评分 | `rubrics.py` + `rubric_base.py` | 整局游戏的 Benchmark 综合评分（排行榜用） |

辅助部分：

- `scripts/generate_damage_matrix.py`：**开发期代码生成器**，从 OpenRA 仓库的 YAML 定义自动生成 `damage_matrix.py`，保证静态数据与真实游戏一致。
- `tests/`：三个业务模块的完整单元测试（数据一致性、奖励各维度正确性、评分公式正确性）。
- `pyproject.toml`：setuptools 打包配置，无运行时第三方依赖（纯标准库），仅 dev 依赖 pytest。

设计上刻意区分了**两套评分系统**（文件内注释明确说明）：

- **奖励向量**（`reward_vector.py`）：逐 tick、稠密、基于增量的训练信号，用于强化学习策略优化，回答"智能体每一步做得怎么样"。
- **Benchmark 评分**（`rubrics.py`）：整局终结后的综合分数（0-100），用于排行榜排名，回答"智能体这局整体表现如何"。

## 2. 各文件主要功能

### 2.1 项目配置 — `pyproject.toml`

- 定义包元数据：名称 `openra-rl-util`、版本 `0.1.0`、描述"reward vector, damage matrix, scoring rubrics"。
- 要求 Python >= 3.10，运行期零第三方依赖。
- dev 可选依赖：`pytest>=7.0`、`pytest-asyncio>=0.21`。
- 构建后端为 setuptools + setuptools-scm。

### 2.2 包入口 — `openra_rl_util/__init__.py`

- 声明 `__version__ = "0.1.0"`。
- 文档注释列出包的三个组成部分：`reward_vector`、`damage_matrix`、`rubrics`。

### 2.3 静态数据层 — `openra_rl_util/damage_matrix.py`

> 文件头注明"由 `scripts/generate_damage_matrix.py` 自动生成，禁止手工编辑"。

**数据表（常量）：**

- `UNIT_ARMOR` / `BUILDING_ARMOR`：每个单位/建筑的护甲类型。红警共有 5 类护甲：`none`（步兵）、`light`（轻型载具/飞机/潜艇）、`heavy`（坦克/重型载具）、`wood`（木质建筑）、`concrete`（混凝土结构）。
- `UNIT_COST` / `BUILDING_COST`：单位与建筑的造价（用于按成本加权计算奖励）。
- `UNIT_EFFECTIVENESS`：各作战单位对 5 类护甲的伤害倍率（1.0 = 100%，>1 加成，<1 削弱），源自 `mods/ra/weapons/*.yaml` 的 `Versus` 段；非作战单位为 `{}`。
- `DEFENSE_EFFECTIVENESS`：防御建筑（碉堡、特斯拉塔、SAM 等）的对各类目标效能，供"破坏"维度使用。
- 特殊角色集合：`ECONOMIC_UNITS`（harv/truk）、`ECONOMIC_BUILDINGS`（proc/silo）、`PRODUCTION_BUILDINGS`（兵营/工厂/机场等）、`TECH_BUILDINGS`、`POWER_BUILDINGS`、`NON_COMBAT_UNITS`（由 `UNIT_EFFECTIVENESS` 动态推导）。

**查询函数（供上层调用）：**

- `get_effectiveness(attacker, target_armor)`：查单点伤害倍率。
- `get_unit_vs_unit(attacker, target)`：通过目标护甲间接求单位对单位克制。
- `get_unit_armor` / `get_building_armor`：护甲查询（未知类型给默认值）。
- `get_unit_cost` / `get_building_cost`：造价查询。
- `can_attack(unit_type)`：判断能否攻击。
- `is_economic_target` / `is_production_target` / `is_tech_target` / `is_power_target`：目标战略价值分类。
- `compute_army_counter_score(own_units, enemy_units)`：按敌方护甲分布加权的**兵力克制评分**，返回 `(counter_score, vulnerability_score)`，即己方有效作战单位占比与脆弱单位占比。

### 2.4 训练信号层 — `openra_rl_util/reward_vector.py`

依赖 `damage_matrix` 提供的全部数据与查询函数。三个核心数据结构：

- **`RewardVector`**（dataclass）：7+1 维奖励向量，8 个维度为
  1. `combat` — 按成本加权的战损交换（含 HP 部分伤害跟踪）
  2. `economy` — 经济成长与战争（现金/矿石/资产增量、敌方矿车/精炼厂摧毁奖励、己方矿车损失惩罚）
  3. `infrastructure` — 基地建设/科技进程/生产利用率（新建筑类型奖励、生产线利用率、电力健康度）
  4. `intelligence` — 侦察/开雾/威胁发现（新敌兵首次发现、敌方建筑类型发现、基地位置发现）
  5. `composition` — 兵力结构质量（调用 `compute_army_counter_score`）
  6. `tempo` — 行动时间效率（闲置作战单位惩罚、指令速率奖励）
  7. `disruption` — 战略破坏（敌方电厂/生产线/科技建筑被摧毁）
  8. `outcome` — 终局胜负信号（仅 `done` 时 +1/-1）
  并提供 `as_dict()` / `as_array()` / `weighted_scalar(weights)`（用默认权重或自定义权重折叠成标量，供单值 head 的 RL 算法使用）。

- **`RewardVectorState`**（dataclass）：跨 tick 状态跟踪，包括各类计数器的上一帧值、单位/建筑 HP 快照、已发现敌人 ID 集合、已建造建筑类型集合等——这是"增量奖励"（delta-based）计算的基础。

- **`RewardVectorComputer`**：主计算器。`compute(obs)` 接收完整观测字典（含 military/economy/units/buildings/visible_enemies/visible_enemy_buildings/production_queues/done/result 等键），依次调用 7 个私有 `_compute_*` 函数填充各维度，并在每帧结束后调用 `_update_state()` 刷新状态；所有维度均夹取到 [-1, 1]。`reset()` 用于新 episode 清空状态。

另定义 `DEFAULT_WEIGHTS`（combat 0.30、economy 0.15、disruption 0.15、其余各 0.10、outcome 1.00）和一组归一化/奖励常量（`COMBAT_NORMALIZER`、`HARVESTER_BONUS` 等）。

### 2.5 抽象基类层 — `openra_rl_util/rubric_base.py`

从 OpenEnv 项目 vendored 的极简 Rubric 基类（BSD 协议，文件头注明当 `openenv.core.rubrics` 发布后可用 re-export 替换）。提供：

- **`Rubric`**（ABC）：统一 `__call__(action, observation)` 接口（内部转发到 `forward`），记录 `last_score`，定义 `reset` / `state_dict` / `load_state_dict`。
- **`TrajectoryRubric`**：累积整个 episode 的 `(action, observation)` 轨迹，非终局返回 `intermediate_reward`（默认 0.0），终局（`done=True`）调用子类实现的 `score_trajectory()`。
- **`ExponentialDiscountingTrajectoryRubric`**：提供指数折扣的 `compute_step_rewards()`，逐帧奖励 `r_t = gamma^(T-1-t) * R_final`，做信用分配。
- **`WeightedSum`**：多 Rubric 加权组合器（校验权重和为 1.0），`forward` 时逐个子 rubric 打分再加权求和。

### 2.6 评估评分层 — `openra_rl_util/rubrics.py`

基于 `rubric_base` 的 OpenRA-Bench 评分实现，文件头再次强调与奖励向量是两套互补系统：

- **`OpenRAWinLossRubric`**：终局胜负评分（win +1.0 / lose -1.0 / draw 0.0），带 `gamma` 时间折扣。
- **`MilitaryEfficiencyRubric`**：军事实效，`kills_cost / (kills_cost + deaths_cost)`，无战斗时给中性分 0.5。
- **`EconomyRubric`**：经济分，`assets_value / (assets_value + 10000)`（Sigmoid 型归一化）。
- **`OpenRABenchRubric`**：`WeightedSum` 组合——50% 胜负 + 25% 军事 + 25% 经济，并暴露 `win_loss` / `military` / `economy` 命名引用。
- **`compute_game_metrics(final_obs)`**：从终局观测提取结果字典（result/win/ticks/kills_cost/deaths_cost/kd_ratio/assets_value/cash）。
- **`compute_composite_score_from_games(game_results)`**：多局聚合的综合评分（0-100）。采用"先算每局子分再取平均"（而非先合并再归一化），以避免 Jensen 不等式在多局聚合时的失真；注释声明这是 Benchmark 评分的唯一事实来源（single source of truth）。

### 2.7 代码生成器 — `scripts/generate_damage_matrix.py`

开发期工具，从 OpenRA 仓库实际 YAML 生成 `damage_matrix.py`，保证静态数据与游戏真实值一致。默认读取 `../OpenRA-RL/OpenRA`，支持 `--openra-path` 与 `--output` 覆盖，`--verify` 只比对不写入（CI 校验用）。内部结构：

- **MiniYAML 解析**：`parse_miniyaml()`（基于 tab 缩进的嵌套解析）、`resolve_inherits()`（递归合并 `Inherits` 继承链、处理删除节点）、`deep_merge()` / `deep_copy_dict()`（防缓存别名共享）。
- **数据提取**：`extract_weapons()` / `extract_units()`（按文件名列表读取 YAML 并解析继承）；`extract_versus()`（从武器定义中提取主伤害弹头的 Versus 百分比并转成倍率）；`get_primary_weapon()` / `get_secondary_weapon()` / `get_cost()` / `get_armor_type()`。
- **特殊建模规则**（YAML 原始 Versus 无法准确表达目标限制时人工修正）：
  - `SPECIAL_MODELING`：谭雅（e7）、间谍（spy）、军犬（dog）、潜艇（ss）等目标受限单位。
  - `DUAL_WEAPON_UNITS`：4tnk 双武器取平均。
  - `GROUND_WEAPON_OVERRIDE`：e3、heli 主武器为对空，地面战用副武器。
  - `DEFENSE_WEAPON_OVERRIDE`：碉堡等驻军防御建筑使用驻军武器。
- **代码生成**：`format_dict()` / `generate_module()` 拼接出 `damage_matrix.py` 的源码字符串。
- **main**：校验 OpenRA 路径 → 解析武器与单位 → 构建各数据表并打印调试行 → 生成源码 → 写出（或 verify 比对）。

### 2.8 测试层 — `tests/`

- **`conftest.py`**：共享夹具。`empty_obs`（空观测：所有字段为 0/空）、`basic_obs`（含小型基地与军队的观测，覆盖单位/建筑/可见敌人）、`MockObs`（rubric 测试用的模拟终局观测，属性式访问以贴近真实 GameObservation 对象）。
- **`test_damage_matrix.py`**：验证数据完整性与一致性（每个单位都有护甲/造价、步兵=none 护甲、坦克=heavy、飞机=light）、克制关系查询正确性、`can_attack`、建筑战略分类、`compute_army_counter_score`（空阵/克制/脆弱/混合/非作战单位排除）、成本查询。
- **`test_reward_vector.py`**：验证 `RewardVector` 数据类（默认零向量、as_dict/as_array、加权标量）、`compute` 主流程（胜负结果映射）、7 个维度各自的奖励/惩罚逻辑（击杀加分、伤亡扣分、部分伤害、经济增量、矿车损失、新建筑、电力赤字、侦察发现、兵力克制、闲置惩罚、破坏敌方关键建筑）、多 tick 累积、reset、所有维度 [-1,1] 有界性。
- **`test_rubrics.py`**：验证胜负/军事/经济三个 rubric 的评分公式（含边界与无战斗中性分）、`OpenRABenchRubric` 组合分数、`compute_game_metrics` 指标提取（含 kd_ratio 防零除）、`compute_composite_score_from_games` 聚合评分（空/全胜/全败/混合/单局与 rubric 一致性）。

## 3. 文件之间的层次与连接关系

### 3.1 分层架构

```
┌──────────────────────────────────────────────────────────┐
│  使用方（OpenRA-RL 训练器 / OpenRA-Bench 评估器）          │
└───────────────────┬──────────────────────────────────────┘
                    │ 调用
┌───────────────────▼──────────────────────────────────────┐
│  业务层                                             │
│  reward_vector.py  ──► 依赖 damage_matrix.py        │
│  rubrics.py       ──► 依赖 rubric_base.py           │
└──────────┬──────────────────────────┬───────────────────┘
           │ 依赖                     │ 继承
┌──────────▼──────────┐   ┌───────────▼─────────────────┐
│  数据层             │   │  抽象基类层                 │
│  damage_matrix.py   │   │  rubric_base.py            │
│  （静态数据+查询）  │   │  （OpenEnv vendored）       │
└─────────────────────┘   └─────────────────────────────┘
           ▲
           │ 生成（开发期流水线）
┌──────────┴─────────────────────────────────────────────┐
│  scripts/generate_damage_matrix.py                     │
│  （OpenRA YAML ──► 解析/继承/提取 ──► damage_matrix.py）│
└────────────────────────────────────────────────────────┘

  tests/  ──► 分别测试 damage_matrix / reward_vector / rubrics
             （conftest.py 提供共享观测夹具）
```

### 3.2 依赖关系（自底向上）

1. **`damage_matrix.py` 与 `rubric_base.py` 是"无依赖"的基础层**——前者只含数据与纯查询函数，后者是抽象基类，两者互不引用。
2. **`reward_vector.py` 依赖 `damage_matrix.py`**：导入其全部数据表（`UNIT_COST`、`BUILDING_COST`、`ECONOMIC_UNITS`、`PRODUCTION_BUILDINGS`、`POWER_BUILDINGS`、`TECH_BUILDINGS` 等）与查询函数（`can_attack`、`compute_army_counter_score`、`get_effectiveness`、`get_unit_cost` 等），用于按成本加权战损、判断战略目标类别、计算兵力克制。**奖励向量的"知识"全部来自 damage_matrix。**
3. **`rubrics.py` 依赖 `rubric_base.py`**：`OpenRAWinLossRubric` 继承 `ExponentialDiscountingTrajectoryRubric`，`MilitaryEfficiencyRubric`/`EconomyRubric` 继承 `TrajectoryRubric`，`OpenRABenchRubric` 继承 `WeightedSum`。rubrics 不依赖 damage_matrix（评估只看终局聚合统计量）。
4. **`tests/` 依赖所有业务模块**：`conftest.py` 的 `MockObs` 被 `test_rubrics.py` 直接 import；各测试文件 import 对应模块的公开 API。
5. **`scripts/generate_damage_matrix.py` 独立于运行时模块**：它不 import 包内代码，只产出 `damage_matrix.py` 的源码文本；是"数据流水线"而非"运行时依赖"。

### 3.3 三条数据/协作流程

**流程 A — 数据生成流水线（开发期，一次性/CI 校验）：**

```
OpenRA 仓库 mods/ra/{weapons,rules}/*.yaml
        │ scripts/generate_damage_matrix.py
        │  （MiniYAML 解析 → Inherits 继承合并 → Versus 提取 → 特殊建模修正）
        ▼
damage_matrix.py（纯静态数据 + 查询函数，禁止手改）
```

**流程 B — 训练期信号流（每 tick）：**

```
游戏观测 obs（military/economy/units/buildings/visible_*）
        │ RewardVectorComputer.compute(obs)
        ▼
RewardVector（7+1 维）──► weighted_scalar() ──► 标量（单值 head RL）
        │                    或直接使用多维向量（多 head RL）
        ▼
RewardVectorState 更新（为下一 tick 的增量计算做准备）
```

**流程 C — 评估期评分流（每局终局 / 多局聚合）：**

```
整局 (action, obs) 轨迹
        │ rubrics（OpenRABenchRubric = 50%胜负 + 25%军事 + 25%经济）
        ▼
compute_game_metrics(final_obs) ──► 单局指标
        │ compute_composite_score_from_games()
        ▼
0-100 综合评分（排行榜）
```

### 3.4 协作要点

- **数据与逻辑解耦**：所有"游戏常识"（护甲、造价、克制、战略分类）集中在 `damage_matrix.py`，奖励逻辑 `reward_vector.py` 只写"如何用这些知识打分"，改游戏数据无需动奖励代码；反之改奖励权重也不影响数据。
- **两套评分互补**：`reward_vector.py` 训练技能（逐 tick 稠密信号），`rubrics.py` 度量表现（整局稀疏信号）——文件注释明确"reward vector 训练技能，benchmark 衡量整体表现"。
- **生成器保证数据一致**：OpenRA 游戏版本升级时只需重跑 `scripts/generate_damage_matrix.py`（或 `--verify` 校验），避免手工维护几百行数据出错。
- **基类复用**：`rubric_base.py` 完全按 OpenEnv API 设计，未来可直接替换为官方包导入，业务代码零改动。
